<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 dark:text-gray-200 leading-tight">
            Criar Novo Board
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-6 text-gray-900 dark:text-gray-100">
                    <form method="POST" action="{{ route('boards.store') }}">
                        @csrf

                        <div>
                            <x-input-label for="name" value="Nome do Board" />
                            <x-text-input id="name" class="block mt-1 w-full" type="text" name="name" :value="old('name')" required autofocus />
                            <x-input-error :messages="$errors->get('name')" class="mt-2" />
                        </div>

                        <div class="mt-4">
                            <x-input-label for="description" value="Descrição (Opcional)" />
                            <textarea id="description" name="description" rows="4" 
                                      class="block mt-1 w-full bg-gray-900 border-transparent focus:border-blue-500 focus:ring-blue-500 rounded-md shadow-sm text-gray-300 placeholder-gray-500">{{ old('description') }}</textarea>
                            <x-input-error :messages="$errors->get('description')" class="mt-2" />
                        </div>

                        <div class="flex items-center justify-end mt-6">
                            <x-cancelar href="{{ route('dashboard') }}">
                                Cancelar
                            </x-cancelar>

                            <x-primary-button>
                                Salvar Board
                            </x-primary-button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>